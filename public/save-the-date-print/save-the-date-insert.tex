% Save the Date Insert — Print-Ready 4-up on A4
% Compile with: xelatex save-the-date-insert.tex

\documentclass[a4paper]{article}

% ============================================================================
% Packages
% ============================================================================
\usepackage[a4paper, margin=0pt]{geometry}
\usepackage{fontspec}
\usepackage{graphicx}
\usepackage{xcolor}
\usepackage{tikz}
\usepackage{calc}

% ============================================================================
% Page Setup
% ============================================================================
\pagestyle{empty}
\setlength{\parindent}{0pt}
\setlength{\parskip}{0pt}

% ============================================================================
% Fonts (system-installed in ~/Library/Fonts/)
% ============================================================================
\setmainfont{Cormorant Garamond}[
    BoldFont = {Cormorant Garamond Bold},
]
\newfontfamily\ephesis{Ephesis}
\newfontfamily\montserrat{Montserrat}

% ============================================================================
% Colors
% ============================================================================
\definecolor{bodytext}{HTML}{555555}
\definecolor{subtitle}{HTML}{777777}

% ============================================================================
% Card dimensions: A6 = 105mm x 148.5mm (exactly 1/4 of A4)
% No side trimming needed — just one vertical + one horizontal cut
% ============================================================================
\newlength{\cardwidth}
\newlength{\cardheight}
\setlength{\cardwidth}{105mm}
\setlength{\cardheight}{148.5mm}

% No centering offsets needed — cards tile exactly to A4
\newlength{\xoffset}
\newlength{\yoffset}
\setlength{\xoffset}{0pt}
\setlength{\yoffset}{0pt}

% ============================================================================
% Single card content as a TikZ picture
% ============================================================================
\newcommand{\savethedate}{%
\begin{tikzpicture}[x=1mm, y=1mm]
    % Card boundary: 105mm x 148.5mm (A6)
    % Content shifted up to stay clear of ~5mm unprintable printer margins
    \useasboundingbox (0,0) rectangle (105,148.5);

    % --- "save the date" in Ephesis ---
    \node[anchor=center] at (52.5, 128)
        {\ephesis\fontsize{40pt}{44pt}\selectfont save the date};

    % --- Thin rule ---
    \draw[black, line width=0.4pt] (20, 118) -- (85, 118);

    % --- Names in Montserrat ---
    \node[anchor=center] at (52.5, 109)
        {\montserrat\fontsize{8pt}{10pt}\selectfont
         \addfontfeature{LetterSpace=12}%
         HILARY BRANNON};
    \node[anchor=center] at (52.5, 100)
        {\montserrat\fontsize{8pt}{10pt}\selectfont
         \addfontfeature{LetterSpace=3}%
         \&};
    \node[anchor=center] at (52.5, 91)
        {\montserrat\fontsize{8pt}{10pt}\selectfont
         \addfontfeature{LetterSpace=12}%
         BRUCE DOMBROWSKI};

    % --- Thin rule ---
    \draw[black, line width=0.4pt] (20, 82) -- (85, 82);

    % --- Date block ---
    \node[anchor=center] at (52.5, 72)
        {\montserrat\fontsize{9pt}{11pt}\selectfont
         \addfontfeature{LetterSpace=10}%
         NOVEMBER};
    \node[anchor=center] at (52.5, 56)
        {\fontsize{44pt}{44pt}\selectfont\bfseries 6};
    \node[anchor=center] at (52.5, 43)
        {\montserrat\fontsize{9pt}{11pt}\selectfont
         \addfontfeature{LetterSpace=10}%
         2026};

    % --- QR Code ---
    \node[anchor=center] at (52.5, 28)
        {\includegraphics[width=15mm]{qrcode.png}};

    % --- QR subtitle ---
    \node[anchor=center] at (52.5, 18)
        {\montserrat\fontsize{7.5pt}{10pt}\selectfont
         \addfontfeature{LetterSpace=8}%
         ADD TO YOUR CALENDAR \& MORE DETAILS};

\end{tikzpicture}%
}

% ============================================================================
% Document
% ============================================================================
\begin{document}

\begin{tikzpicture}[remember picture, overlay]
    % Reference from top-left of page, shifted 4mm up to compensate for
    % HP Envy 5055 having a larger unprintable bottom margin than top
    \coordinate (origin) at ([xshift=\xoffset, yshift=-\yoffset+4mm] current page.north west);

    % --- Card 1: top-left ---
    \node[anchor=north west, inner sep=0pt] at (origin) {\savethedate};

    % --- Card 2: top-right ---
    \node[anchor=north west, inner sep=0pt] at ([xshift=\cardwidth] origin) {\savethedate};

    % --- Card 3: bottom-left ---
    \node[anchor=north west, inner sep=0pt] at ([yshift=-\cardheight] origin) {\savethedate};

    % --- Card 4: bottom-right ---
    \node[anchor=north west, inner sep=0pt] at ([xshift=\cardwidth, yshift=-\cardheight] origin) {\savethedate};

    % No cut guides needed — just cut A4 in half twice

\end{tikzpicture}

\end{document}
